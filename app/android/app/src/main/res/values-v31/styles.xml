<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash: gold background, NO visible icon (transparent
         animated icon). The Flutter splash paints the circle mark + wordmark on
         first frame; NormalTheme is gold too so the native→Flutter handover has no
         flicker. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
